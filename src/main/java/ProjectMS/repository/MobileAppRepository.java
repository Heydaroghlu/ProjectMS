package ProjectMS.repository;

import ProjectMS.model.MobileApp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MobileAppRepository extends JpaRepository<MobileApp, Integer> {
}
